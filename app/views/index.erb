<!DOCTYPE html>
    <head>
        <title>Recipes</title>
    </head>
    <body>
        <ul>
            <% @recipes.each do |recipe| %>
                <li><a href = <%= "/recipes/#{recipe.id}" %>><%= recipe.name %></a></li>
            <% end %>
        </ul>
    </body>
</html>