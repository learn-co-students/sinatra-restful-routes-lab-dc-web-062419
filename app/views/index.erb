<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>New Recipe</title>
  </head>
  <body>
    <h1>All Recipes: </h1>
    <ul>
    <% @recipes.each do |recipe| %>
    <li><a href= <%= "/recipes/#{recipe.id}" %>><%= recipe.name %></a></li>
    <% end %>
    </ul>

    <br>

  </body>
</html>